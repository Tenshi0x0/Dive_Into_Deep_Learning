## Q5

Model generalization error refers to the situation where the training set size is $\frac{K-1}{K} n$ instead of $n$.

## Q6

Considering sign only will lead to ignoring the magnitude of function values, which may fail to capture the true complexity. (e.g. Both sign pattern of $f(x_1)=114, f(x_2)=-514$ and $g(x_1)=1919810, g(x_2)=-998244353$ are $(+, -)$)